# 浏览器原理与性能优化面试常见坑与避坑指南

## 核心知识点

### 浏览器渲染问题

浏览器渲染的常见问题：

1. **重排重绘**：频繁的 DOM 操作导致性能问题
2. **渲染阻塞**：CSS 和 JS 阻塞渲染
3. **合成层**：没有正确使用合成层

### 性能优化误区

性能优化的常见误区：

| 问题场景 | 说明 |
|----------|------|
| 过度优化 | 过早优化，影响开发效率 |
| 优化方向错误 | 优化了非瓶颈代码 |
| 忽略用户体验 | 只关注技术指标，忽略用户感受 |

### 网络请求问题

网络请求的常见问题：
- 请求过多
- 没有使用缓存
- 请求顺序不合理

## 代码示例

```html
<link rel="stylesheet" href="styles.css" media="print" onload="this.media='all'">
<link rel="preload" href="critical.js" as="script">
<link rel="prefetch" href="non-critical.js" as="script">

<img src="image.jpg" loading="lazy" alt="Lazy loaded image">
```

```javascript
function debounce(func, wait) {
  let timeout;
  return function(...args) {
    clearTimeout(timeout);
    timeout = setTimeout(() => func.apply(this, args), wait);
  };
}

function throttle(func, limit) {
  let inThrottle;
  return function(...args) {
    if (!inThrottle) {
      func.apply(this, args);
      inThrottle = true;
      setTimeout(() => (inThrottle = false), limit);
    }
  };
}

const observer = new IntersectionObserver((entries) => {
  entries.forEach(entry => {
    if (entry.isIntersecting) {
      const img = entry.target;
      img.src = img.dataset.src;
      observer.unobserve(img);
    }
  });
});

document.querySelectorAll('img[data-src]').forEach(img => {
  observer.observe(img);
});
```

```javascript
const cache = new Map();

async function fetchWithCache(url) {
  if (cache.has(url)) {
    return cache.get(url);
  }
  
  const response = await fetch(url);
  const data = await response.json();
  
  cache.set(url, data);
  return data;
}

const controller = new AbortController();

const fetchData = async (signal) => {
  const response = await fetch('/api/data', { signal });
  return response.json();
};

const request = fetchData(controller.signal);

controller.abort();
```

## 常见问题

### Q: 什么是关键渲染路径？

A: 从 HTML 到首次渲染的过程，包括 HTML 解析、CSS 解析、布局、绘制、合成。

### Q: 如何减少首屏加载时间？

A: 代码分割、懒加载、使用 CDN、优化图片。

## 参考链接

- [Web.dev](https://web.dev/)
- [MDN Web Docs](https://developer.mozilla.org/)
